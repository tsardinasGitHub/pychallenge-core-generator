# Challenge Generator - Dependencies
# Python Version: 3.13.0 (minimum required: 3.8+)

# Currently no external packages are required
# The project uses only Python's standard library

# To set up the development environment:
# 1. Create virtual environment: python -m venv venv
# 2. Activate it: venv\Scripts\activate (Windows) or source venv/bin/activate (Linux/Mac)
# 3. Install requirements: pip install -r requirements.txt

# Future dependencies (uncomment when needed):
# colorama>=0.4.4          # For cross-platform terminal colors
# click>=8.0.0             # To improve CLI interface  
# rich>=12.0.0             # For more attractive terminal interfaces
# pytest>=7.0.0            # For unit testing
# requests>=2.28.0         # To fetch challenges from external APIs
# pyyaml>=6.0              # For configuration in YAML files
# sqlalchemy>=1.4.0        # For more robust database